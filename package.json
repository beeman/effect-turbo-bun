{
  "name": "effect-turbo-bun",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "check-types": "turbo run check-types",
    "dev": "turbo run dev",
    "dev:api": "turbo run dev -F @repo/api",
    "lint": "turbo run lint",
    "test": "vitest run",
    "test:watch": "vitest --watch"
  },
  "devDependencies": {
    "@types/minimatch": "^6.0.0",
    "@vitest/coverage-v8": "3.2.4",
    "jsdom": "^26.1.0",
    "turbo": "^2.5.6",
    "typescript": "5.9.2"
  },
  "engines": {
    "node": ">=18"
  },
  "packageManager": "bun@1.2.21",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "dependencies": {
    "@turbo/gen": "^2.5.6"
  }
}
